import {
     Button,
     Flex,
     Grid,
     GridItem,
     Heading,
     Input,
     Select,
     TabPanel,
     Text,
} from "@chakra-ui/react";

const Profile = () => {
     return (
          <>
               <TabPanel>
                    <Heading variant={"mediumHeading"}>
                         Хувийн мэдэээлэл
                    </Heading>
                   
               </TabPanel>
          </>
     );
};

export default Profile;
